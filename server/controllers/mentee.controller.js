async function getAll(req, res) {
  try {
    const mentees =
  } catch {
    res.status(500);
    res.send('Could not get the list of mentees.');
  }

  console.log('🎯');
}

module.exports = { getAll };
